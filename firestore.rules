rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows anyone to read and write while we are building/testing
      allow read, write: if true;
    }
  }
}